# Database connection
DATABASE_URL=postgres://postgres:postgres@localhost:5432/copyright_detector

# Embedding API keys (at least one required)
# Gemini is the primary embedding model (768 dimensions)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Hugging Face as fallback (512 dimensions)
# HUGGINGFACE_API_KEY=your_huggingface_api_key_here
